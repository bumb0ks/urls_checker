file(GLOB_RECURSE INTERNAL_SOURCES "*.h" "*.cpp" "*.hpp")
set(SOURCES ${SOURCES} ${INTERNAL_SOURCES} PARENT_SCOPE)

HEADER_DIRECTORIES(INTERNAL_INCLUDES ".")
set(INCLUDES ${INCLUDES} ${INTERNAL_INCLUDES} PARENT_SCOPE)